import React from 'react'
import LandingPage from '../../components/LandingPage'

export default function Landing({ initialCurrency, params }) {
  return <LandingPage initialCurrency={initialCurrency} params={params} />
}
